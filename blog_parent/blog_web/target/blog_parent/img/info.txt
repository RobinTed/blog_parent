用户图片存储名称说明
	*_*
	第一位：上传用户的ID
	第二位：上传时间，毫秒
	
文章图片命名格式
	*_*_*_*
	第一位：所属文件夹，可取值：article
	第二位：所属用户ID
	第三位：所属文章ID
	第四位：上传时间，毫秒
	
	图片预览策略：
		由于浏览器出于安全考虑，不能直接得到本地文件的绝对路径
		为了保留前面的设计，采用一个折中的方法，将
	